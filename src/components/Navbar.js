import React from 'react'
import Logo from '../images/airbnb-logo.png'

function Navbar() {
  return (
    <header>
      <nav>
        <img src={Logo} alt='' />
      </nav>
    </header>
  )
}

export default Navbar
